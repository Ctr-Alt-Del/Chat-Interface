import React, { Component } from 'react';
import logo from '../logo.png'

class Header extends Component {
  render() {
    return (
        <div class="headBorder">
          {/* <img src={logo} class="logo" alt="LC Logo"/>        */}
        </div>
    );
  }
}

export default Header;